{
  "name": "fortune-teller-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy:dev": "serverless deploy -s dev -r us-east-1",
    "deploy:prod": "serverless deploy -s prod -r us-east-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "~1.74.1",
    "serverless-python-requirements": "~5.1.0"
  }
}
